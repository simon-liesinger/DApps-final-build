using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class piece_part_helper : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //shrink when the claw is near?
        //if the pieces need to change shape for some reason it goes here
    }
}
